import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightText = Color.fromARGB(255, 104, 103, 103);
  static const solidText = Color.fromARGB(255, 0, 0, 0);

  // Dark theme colors
  static const darkBackground = Color(0xFF121212);
  static const darkAppBar = Color(0xFF3700B3);
  static const darkText = Color(0xFFE0E0E0);
}
